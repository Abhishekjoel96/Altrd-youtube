[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && python3 -m pip install -r requirements.txt && python3 app.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "never" 