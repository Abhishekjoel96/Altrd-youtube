[variables]
NPM_CONFIG_UPDATE_NOTIFIER = 'false'

[phases.setup]
nixPkgs = ['nodejs_18', 'python3', 'ffmpeg']

[phases.install]
cmds = ['npm ci --legacy-peer-deps']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'cd backend && python3 -m pip install -r requirements.txt && python3 app.py' 